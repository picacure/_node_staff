##本周感想

<%= 本周感想 %>

##项目情况

<% for(var j = 0, jlen = 项目.length; j < jlen; j++) { %>

###<%= 项目[j].项目名称 %>

**前端**

<%= 项目[j].前端 %>

**投入时间**

<%= 项目[j].投入时间 %>

**事项**
<% for(var i = 0, ilen = 项目[j].事项.length;i < ilen; i++) { %>
-<%= 项目[j].事项[i].名称 + '(' + 项目[j].事项[i].开始时间 + '至' + 项目[j].事项[i].结束时间 + ')' %>
<% } %>
<% } %>